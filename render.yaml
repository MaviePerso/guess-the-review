services:
  - type: web
    name: guess-the-review-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js

    envVars:
      - key: NODE_VERSION
        value: "20"
